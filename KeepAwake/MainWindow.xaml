<Window x:Class="KeepAwake.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeepAwake"
        mc:Ignorable="d"
        Title="KeepAwake" Height="360" Width="380"
        MinHeight="360" MinWidth="380"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BrandBg}"
        ResizeMode="CanMinimize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource BrandText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#x2615;" FontSize="26" Foreground="{StaticResource BrandAccent}" VerticalAlignment="Center"/>
            <TextBlock Text="KeepAwake" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrandText}"
                       VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Status -->
        <Border Grid.Row="1" Background="{StaticResource BrandSurface}" CornerRadius="8" Padding="14,12" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <Ellipse x:Name="StatusDot" Width="14" Height="14" Fill="{StaticResource BrandMuted}" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" Text="Stopped" Foreground="{StaticResource BrandText}" FontSize="16"
                           FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Options -->
        <StackPanel Grid.Row="2" Margin="2,0,0,8">
            <CheckBox x:Name="DisplayCheck" Content="Keep the display awake too"
                      Foreground="{StaticResource BrandText}" FontSize="14" IsChecked="True"
                      Checked="DisplayCheck_Changed" Unchecked="DisplayCheck_Changed"/>
            <TextBlock Text="Prevents the screen from turning off as well as the PC sleeping."
                       Foreground="{StaticResource BrandMuted}" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="StartButton" Grid.Column="0" Content="Start" Background="{StaticResource BrandAccent}"
                    Click="StartButton_Click"/>
            <Button x:Name="StopButton" Grid.Column="2" Content="Stop" Background="{StaticResource BrandSurface}"
                    Click="StopButton_Click" IsEnabled="False"/>
        </Grid>

        <!-- About link -->
        <TextBlock Grid.Row="5" Margin="0,16,0,0" HorizontalAlignment="Right">
            <Hyperlink Foreground="{StaticResource BrandAccent}" TextDecorations="None"
                       Click="AboutLink_Click">About</Hyperlink>
        </TextBlock>
    </Grid>
</Window>
