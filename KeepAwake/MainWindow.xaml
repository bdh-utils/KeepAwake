<Window x:Class="KeepAwake.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeepAwake"
        mc:Ignorable="d"
        Title="KeepAwake" Height="585" Width="380"
        MinHeight="585" MinWidth="380"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BrandBg}"
        ResizeMode="CanMinimize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource BrandText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#x2615;" FontSize="26" Foreground="{StaticResource BrandAccent}" VerticalAlignment="Center"/>
            <TextBlock Text="KeepAwake" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrandText}"
                       VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Status -->
        <Border Grid.Row="1" Background="{StaticResource BrandSurface}" CornerRadius="8" Padding="14,12" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <Ellipse x:Name="StatusDot" Width="14" Height="14" Fill="{StaticResource BrandMuted}" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" Text="Stopped" Foreground="{StaticResource BrandText}" FontSize="16"
                           FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Options -->
        <StackPanel Grid.Row="2" Margin="2,0,0,8">
            <TextBlock Text="Method" Foreground="{StaticResource BrandText}" FontSize="13"
                       FontWeight="SemiBold" Margin="0,0,0,6"/>
            <RadioButton x:Name="ExecutionStateRadio" GroupName="Method"
                         Content="Block sleep (ask Windows to stay awake)"
                         Foreground="{StaticResource BrandText}" FontSize="13" IsChecked="True"
                         Checked="Method_Changed"/>
            <RadioButton x:Name="MouseWiggleRadio" GroupName="Method"
                         Content="Wiggle the mouse periodically"
                         Foreground="{StaticResource BrandText}" FontSize="13" Margin="0,4,0,0"
                         Checked="Method_Changed"/>
            <StackPanel Orientation="Horizontal" Margin="22,6,0,0">
                <TextBlock Text="Wiggle every" Foreground="{StaticResource BrandText}" FontSize="13"
                           VerticalAlignment="Center"/>
                <TextBox x:Name="WiggleIntervalBox" Text="30" Width="48" Margin="8,0,8,0" FontSize="13"
                         TextAlignment="Center" IsEnabled="False"
                         Background="{StaticResource BrandSurface}" Foreground="{StaticResource BrandText}"
                         BorderBrush="{StaticResource BrandMuted}" CaretBrush="{StaticResource BrandText}"
                         MaxLength="4"
                         TextChanged="WiggleInterval_TextChanged" LostFocus="WiggleInterval_LostFocus"/>
                <TextBlock Text="seconds" Foreground="{StaticResource BrandText}" FontSize="13"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <CheckBox x:Name="DisplayCheck" Content="Keep the display awake too"
                      Foreground="{StaticResource BrandText}" FontSize="13" IsChecked="True" Margin="0,10,0,0"
                      Checked="DisplayCheck_Changed" Unchecked="DisplayCheck_Changed"/>
            <TextBlock x:Name="OptionHint" Text="Prevents the screen from turning off as well as the PC sleeping."
                       Foreground="{StaticResource BrandMuted}" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
            <CheckBox x:Name="StartupCheck" Content="Run when I sign in to Windows"
                      Foreground="{StaticResource BrandText}" FontSize="13" Margin="0,14,0,0"
                      Checked="StartupCheck_Changed" Unchecked="StartupCheck_Changed"/>
            <CheckBox x:Name="StartMinimisedCheck" Content="Start minimised to the tray"
                      Foreground="{StaticResource BrandText}" FontSize="13" Margin="0,8,0,0"
                      Checked="StartMinimisedCheck_Changed" Unchecked="StartMinimisedCheck_Changed"/>
            <CheckBox x:Name="StartActiveCheck" Content="Start keeping awake automatically on launch"
                      Foreground="{StaticResource BrandText}" FontSize="13" Margin="0,8,0,0"
                      Checked="StartActiveCheck_Changed" Unchecked="StartActiveCheck_Changed"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="StartButton" Grid.Column="0" Content="Start" Background="{StaticResource BrandAccent}"
                    Click="StartButton_Click"/>
            <Button x:Name="StopButton" Grid.Column="2" Content="Stop" Background="{StaticResource BrandSurface}"
                    Click="StopButton_Click" IsEnabled="False"/>
        </Grid>

        <!-- About link -->
        <TextBlock Grid.Row="5" Margin="0,16,0,0" HorizontalAlignment="Right">
            <Hyperlink Foreground="{StaticResource BrandAccent}" TextDecorations="None"
                       Click="AboutLink_Click">About</Hyperlink>
        </TextBlock>
    </Grid>
</Window>
